services:
  general.app:
    build: .
    container_name: general.app
    ports:
      - "8081:8081"
    env_file:
      - .env
    volumes:
      - .:/app
      - ./data:/app/data
    command: uvicorn src.api.main:app --host 0.0.0.0 --port 8081
